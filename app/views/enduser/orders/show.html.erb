<div class ="left-space">

	<br>

	<h3>購入者：<%= @order.first_name %><%= @order.last_name %></h3>

	<h3>購入日：<%= @order.created_at.strftime("%Y-%m-%d") %></h3>

	<h3>送付先：<%= @order.address %></h3>

	<h3>ステータス：<%= @order.status %></h3>

</div>

<div class="contaiber">
　　<div class="row">
		<table class="table">
		　　<thead></thead>
			<tbody>
				<% @order.order_products.each do |product| %>

				    <th><h5><%= product.cd.title %></h5></th>

					<th><h5><%= product.order_count %>個</h5></th>

					<th><h5>小計：￥<%= product.subtotal_price %></h5></th>

				  </tr>
				<% end %>
					<tr>
						<th><h5>購入金額合計：<%= @order.total_price %>(税込)</h5></th>

						<th><h5>送料：￥<%= @order.postage %>を含む</h5></th>
					</tr>
			</tbody>
		</table>
	</div>
</div>